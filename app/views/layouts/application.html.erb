<!DOCTYPE html>
<html lang="en">
  <head>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/icon?family=Material+Icons" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <!-- Add Materialize CSS -->
<%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" %>

<!-- Add Materialize JavaScript -->
<%= javascript_include_tag "https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js" %>


    <title>DogScouter</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div class="banner">
      <div class="banner-content">
        <h1>Welcome to Dog Scouter</h1>
        <p>A place for cyclists to warn eachother about loose dogs on their rides</p>
      </div>
    </div>

    <% flash.each do |key, message| %>
      <div class="card-panel <%= key == 'notice' ? 'teal lighten-2' : 'red lighten-2' %>">
        <span class="white-text"><%= message %></span>
      </div>
    <% end %>

    <%= yield %>


    <div id="index-banner" class="parallax-container">

    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
  </body>
</html>
